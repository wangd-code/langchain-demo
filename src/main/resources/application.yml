server:
  port: 8080

spring:
  application:
    name: langchain4j-demo
  config:
    import: optional:classpath:application-secrets.yml

# ==========================================
# AI 模型配置（三选一，取消对应注释即可）
# ==========================================

# --- 方案1: Ollama 本地模型（推荐入门，免费，离线可用）---
#ai:
#  model:
#    base-url: http://localhost:11434/v1
#    api-key: ollama
#    model-name: qwen2.5:3b
#    temperature: 0.7
#    max-tokens: 2048

# --- 方案2: DashScope / SiliconFlow（国内推荐，需要 API Key）---
#ai:
#  model:
#    base-url: https://api.siliconflow.cn/v1
#    api-key: ${ai.model.siliconflow-api-key}
#    model-name: Qwen/Qwen3.5-4B
#    temperature: 0.2
#    max-tokens: 200

# --- 方案3: 智谱 BigModel（国内，需要 API Key）---
#ai:
#  model:
#    base-url: https://open.bigmodel.cn/api/paas/v4
#    api-key: ${ai.model.zhipu-api-key}
#    model-name: glm-4.7-flash
#    temperature: 0.3
#    max-tokens: 200

# --- 方案4: DeepSeek（当前使用）---
ai:
  model:
    base-url: https://api.deepseek.com/v1
    api-key: ${ai.model.deepseek-api-key}
    model-name: deepseek-chat
    temperature: 0.3
    max-tokens: 1024

logging:
  level:
    dev.langchain4j: DEBUG
    com.power.ai: DEBUG
